{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Environment": {
    "Debug": "${ENVIRONMENT_DEBUG}",
    "Services": "${ENVIRONMENT_SERVICES_PROD}"
  },
  "AWS": {
    "Profile": "${AWS_PROFILE}",
    "Region": "${AWS_REGION}"
  },
  "DomainUrl": "${DOMAIN_URL_PROD}",
  "ApplicationDomainUrl": "${APPLICATION_DOMAIN_URL_PROD}"
}
